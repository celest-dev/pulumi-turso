name: provider-turso-native
runtime: yaml
plugins:
  providers:
    - name: turso
      path: ../../bin

resources:
  databaseTag:
    type: random:RandomId
    properties:
      byteLength: 4
  database:
    type: turso:Database
    properties:
      group: test
      name: test-${databaseTag.hex}
