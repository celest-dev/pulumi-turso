name: provider-turso-native
runtime: yaml
plugins:
  providers:
    - name: turso
      path: ../../bin

resources:
  database:
    type: turso:Database
    properties:
      group: test
      name: test-xyz

outputs:
  output:
    value: ${database.hostname}
